import { QuartzComponentProps } from "./types";

const CustomHeader = (props: QuartzComponentProps) => {
  return (
    <header>
      <h1>Mindset For Creative Writers ✍🏻🧠</h1> {/* Replace with your desired title/logo */}
    </header>
  );
};

export default CustomHeader;